# structure-sync

something like [FreeFileSync](https://freefilesync.org/) but with less copying files across directories (good for USB sticks if you have roughly the same amount of files but different folder structures)

`structure-sync --help`
